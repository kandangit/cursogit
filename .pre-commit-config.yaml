repos:
  - repo: local
    hooks:
    - id: pre-commit_JSON_local
      name: "El meu pre-commit hook per comprovar el format JSON"
      entry: ./hooksLocals/pre-commit_JSON_local
      language: system